<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
    x:Class="MtbMate.Controls.MapControl">
    <ContentView.Content>
        <Grid>
            <maps:Map
                x:Name="map"
                InitialCameraUpdate="{Binding InitalCamera}"
                HasScrollEnabled="{Binding IsReadOnly, Converter={StaticResource InvertBoolConverter}}"
                HasRotationEnabled="{Binding IsReadOnly, Converter={StaticResource InvertBoolConverter}}"
                MyLocationEnabled="{Binding IsShowingUser}"
                MapClicked="Map_MapClicked"/>
        </Grid>
    </ContentView.Content>
</ContentView>