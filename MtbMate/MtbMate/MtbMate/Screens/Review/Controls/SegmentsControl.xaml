<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:MtbMate.Controls"
    x:Class="MtbMate.Screens.Review.Controls.SegmentsControl"
    Title="Segments">
    <ContentPage.Content>
        <ListView
            ItemsSource="{Binding Attempts}"
            ItemTapped="Attempt_Tapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <controls:MedalsCell
                            Text="{Binding Segment.DisplayName}"
                            Detail="{Binding FormattedTime}"
                            Medal="{Binding Medal}" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>