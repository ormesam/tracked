<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Tracked.Controls.MedalsCell">
    <Grid
        RowSpacing="0"
        Padding="10,0,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            x:Name="TitleLabel" />

        <Label
            Grid.Row="1"
            x:Name="DetailLabel" />

        <StackLayout
            Grid.Column="1"
            Grid.RowSpan="2"
            Orientation="Horizontal"
            HeightRequest="20"
            VerticalOptions="Center"
            x:Name="medals">
        </StackLayout>
    </Grid>
</ContentView>