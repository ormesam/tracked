<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Tracked.Screens.Settings.Modals.JumpAboutModal"
    BackgroundColor="Transparent">
    <ContentPage.Content>
        <Grid>
            <BoxView
                BackgroundColor="{StaticResource SecondaryBackgroundColour}"
                Opacity=".9"/>
            <StackLayout
                Margin="50"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Spacing="15">

                <Label
                    Text="{Binding Title}"
                    FontSize="Title"
                    HorizontalTextAlignment="Center"/>

                <Label
                    Text="You must attach your device directly to your bike to use this feature. Jumps are detected using your phones accelerometer, tap below to find out more." />
                
                <Button
                    Text="Tell me more"
                    BackgroundColor="{StaticResource BackgroundColour}"
                    Clicked="More_Tapped"/>

                <Button
                    Text="Ok"
                    BackgroundColor="{StaticResource Green}"
                    Clicked="Ok_Tapped"/>
                </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>